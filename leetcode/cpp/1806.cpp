class Solution {
public:
    int reinitializePermutation(int n) {

        vector<int> origin;
        for(int i = 0; i < n; ++i)
            origin.emplace_back(i);
        vector<int> cur(origin);

        int ans = 1;
        for(int i = 0; i < n; ++i)
        {
            if(i & 1)
            {
                cur[i] = 
            }
        }
    }
};